# Example Batch Experiment: Deep Learning Comparison
name: "deep_learning"
num_runs: 30
datasets: ["species", "part", "oil", "cross-species"]
models: ["transformer", "rbn", "role_filler", "ensemble", "moe", "cnn", "rcnn", "lstm", "kan"] # mamba
benchmark: true
figures: true
statistical: true
wandb_log: true
overrides:
  epochs: 100
  early_stopping: 10
  learning_rate: 0.0005
